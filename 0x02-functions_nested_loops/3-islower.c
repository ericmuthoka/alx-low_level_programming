#include "main.h"

/**
 * _islower - a program that checks for lowercase characters
 * @c: the parameter
 * Return: return 1 if lowecase
 * otherwise return 0
 */

int _islower(int c)
{

	if (c >= 'a'; c <= 'z')
		return (1);
	else
		return (0);
}
